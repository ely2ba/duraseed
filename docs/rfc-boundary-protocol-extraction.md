# RFC: extract the deterministic boundary protocol
Problem: the complete two-block cohort and confirmation logic remains embedded in prohibited archived `tinker_*` runners, so a complete sub-300-line v1 runner cannot compose the carried instrument; three-cohort freeze evidence does not exist yet.
Alternatives: copy the archived runners; reimplement the protocol inside the runner; ship a plan-only runner; or extract only the deterministic scientific/provenance functions.
Smallest change: authorize one boundary-only semantic extraction of the currently executable cohort and confirmation path into fresh sub-400-line `data/boundary_*` modules; remote clients, authorization, billing, checkpoints, writes, CLI orchestration, and the unverified three-cohort reduction are deferred.
Equivalence gate: old and new code must produce identical initial/Extension-1 manifests, family order, indices, lineage, leakage/capacity audits, refinement selections, confirmation inputs, and replayed 15-pass/23-fail decisions; the eventual three-cohort freeze must also compare exactly before use.
Status: accepted by Ely on 2026-08-12, boundary extraction only.  Extract and
equivalence-verify the Extension-1 path before starting calibration closure;
record old-vs-new fixture and deterministic-stream evidence in `docs/`.  The
three-cohort freeze remains fail-closed and unimplemented until the
completed-evidence boundary-freeze gate extracts
and compares it against complete three-cohort evidence before use.
Any divergence stops; this approval does not authorize calibration changes,
wholesale runner carry-over, remote access, or any threshold/task/selection
change.

# RFC: complete the local calibration instrument
Problem: v1 omitted the pure teacher-dose and Stage-A decision reducers, while teacher allocation mixes pure matching with archived renderer/source-authentication imports; a complete calibration runner cannot reproduce the frozen gates from the current carry-over.
Alternatives: duplicate selection rules in a sub-300-line runner; hide them behind callbacks; carry archived remote runners; or extend the instrument with the exact pure reducers and an isolated allocation core.
Smallest change: carry `training/teacher_dose.py` and `training/stage_a_calibration.py` bit-for-bit, then semantics-preservingly extract the pure crossed-allocation core and exact local random-universe freezer from `teacher_allocation.py` and `teacher_allocation_freeze.py`; fresh runtime supplies authenticated source objects and role-colon token measurement.
Equivalence gate: old and new code must return byte-identical canonical dose decisions, Stage-A LR/duration decisions, teacher candidates, allocations, match reports, and complete freeze results on the existing fixtures and deterministic candidate stream; fixture-only test edits require identical old/new objects and unchanged assertions.
Status: accepted by Ely on 2026-08-12, after the boundary extraction commit; this does not authorize archived remote-runner carry-over, MAPS recalibration, remote access, runtime/runner edits, or any scientific-rule change, and any divergence hard-stops this closure.
